import { Button } from '@windmill/react-ui';
import React from 'react';

export const AddVehicleButtons = () => {
  return (
    <div className='flex items-center '>
      <Button size='regular'>Add Projects</Button>
    </div>
  );
};
